{
  "schemaVersion": 7,
  "reviewedAgainstCommit": "working-tree-2026-08-18-account-export",
  "policyStatus": "engineering inventory; the 2026-08-04 public-beta notice adopts enforced lifetimes and active-account/verified-request retention criteria; control-plane export and erasure are now self-service, while export or deletion of anything outside the control plane, cross-account deletion orchestration, jurisdiction-specific legal holds, and external-resource disposition remain explicit limitations",
  "systems": [
    {
      "system": "Ghostbuild control-plane D1",
      "owner": "DOUS SOFTWARE INC. (controller)",
      "categories": [
        "account identity and profile",
        "authentication sessions and OAuth state",
        "encrypted Cloudflare credential records",
        "Cloudflare account connection metadata and granted scopes",
        "Computer runtime locators"
      ],
      "secrets": "OAuth tokens are encrypted; ciphertext, IVs, session token hashes, and credential handles must never be exported",
      "currentRetention": "OAuth state expires after 10 minutes, authentication sessions after 30 days, and unreferenced credentials become eligible after 24 hours; 15-minute bounded maintenance can be delayed by backlog or retries; other records remain while the account/service is active or until no longer needed or a verified request is fulfilled, with no inactivity purge",
      "currentExport": "a re-authentication-gated settings control (POST /api/account/export) returns every operator-held record for the account as JSON carrying a schema version and an export timestamp — account identity and profile, Cloudflare connection metadata and granted scopes, the existence, storage time, and key version of the encrypted credential record, the Computer runtime locator, and authentication-session and OAuth-state records; it requires the same same-origin authenticated session with a Cloudflare sign-in within the last 10 minutes that erasure requires, because one response discloses the whole account record; ciphertext, initialisation vectors, credential handles, session token hashes, and OAuth provider session identifiers are never exported; each section is bounded at 200 rows and reports the untruncated count and a truncation flag, so a whole export is bounded at 402 records; each section is read independently and a section that cannot be read is named in the document, which then reports itself incomplete rather than emitting a partial export that looks complete; it covers the control plane only and names in the file what it omits and where to get it instead; users can still download individual project source with Download code in the project header and start a privacy or account-data request through the sanitized public Support form, after which a maintainer may arrange a private method; the settings \"Your data\" card states plainly what the operator holds versus what the connected Cloudflare account holds",
      "currentDeletion": "a re-authentication-gated settings control (POST /api/account/delete) erases every operator-held row for the account — account identity, authentication sessions, encrypted credential records, connection metadata and granted scopes, and the Computer runtime locator — and asks Cloudflare to revoke the OAuth grant; it requires a Cloudflare sign-in within the last 10 minutes, an exact typed confirmation phrase, and an explicit acknowledgement that user-owned Cloudflare resources are retained; it is idempotent, never touches user-owned Cloudflare resources or browser storage, and reports an unconfirmed revocation so the user can remove the authorization in Cloudflare; a user who already revoked authorization cannot sign in and must use the Support request path; deleted D1 state remains provider-recoverable through the current Paid-plan 30-day Time Travel window and must not be intentionally restored after erasure"
    },
    {
      "system": "User-owned workspace runtime D1",
      "owner": "user Cloudflare account",
      "categories": [
        "chat catalog and subchat metadata",
        "transcript identities, branch ancestry, and chat visibility",
        "deployment plans, approvals, status, resources, and errors",
        "agent garbage-collection candidates"
      ],
      "secrets": "runtime capabilities and Cloudflare access tokens must never be exported",
      "currentRetention": "individual chat deletion is soft deletion followed by bounded Agent garbage collection; no account-wide retention schedule exists",
      "currentExport": "individual project source can be downloaded; the self-service account export covers the operator control plane only and deliberately excludes this system, because reading it would add a cross-tenant call to a flow kept free of one and this runtime may be unreachable exactly when it matters",
      "currentDeletion": "project removal soft-deletes the chat catalog and asynchronously destroys Agent/workspace state, but chat catalog, transcript, and deployment D1 rows remain; no account-wide user-runtime deletion operation exists; D1 Time Travel recovery can retain deleted state for 7 days on Free or 30 days on Paid and an erasure must be reapplied after any broader recovery"
    },
    {
      "system": "BuilderAgent Durable Objects",
      "owner": "user Cloudflare account",
      "categories": ["chat transcripts", "turn recovery state", "compaction state", "generated titles"],
      "secrets": "credentials and access tokens are not transcript export fields",
      "currentRetention": "deleted project Agents become eligible for bounded user-runtime garbage collection no earlier than 30 minutes after removal; batch limits, backlog, or retries can delay teardown",
      "currentExport": "not implemented; the self-service account export covers the operator control plane only and contains no transcript, turn-recovery, or compaction state",
      "currentDeletion": "project-scoped garbage collection exists; no account-wide runtime deletion operation or deletion-status result exists"
    },
    {
      "system": "ProjectWorkspace Durable Objects and Cloudflare Computer",
      "owner": "user Cloudflare account",
      "categories": ["project files and revisions", "preview state", "validation state", "ephemeral process output"],
      "secrets": "local secret paths, runtime capabilities, and raw command output must never be included in any future account export",
      "currentRetention": "workspace files persist in the user-owned Durable Object; preview processes and tunnels are bounded by runtime lifecycle",
      "currentExport": "project source ZIP only; the self-service account export covers the operator control plane only and contains no workspace files, revisions, or process output",
      "currentDeletion": "project-scoped teardown is not exposed as an account-wide operation"
    },
    {
      "system": "Cloudflare AI inference and AI Gateway request path",
      "owner": "user Cloudflare account; Cloudflare processes inference as the infrastructure provider",
      "categories": [
        "prompts and conversation context needed for the request",
        "relevant project files and tool definitions",
        "model responses and usage metadata"
      ],
      "secrets": "Ghostbuild disables AI Gateway request logging at the gateway, model-request, and request-header layers; credentials and capability secrets must never be model inputs",
      "currentRetention": "Cloudflare AI transiently processes request content to perform inference under Cloudflare's service controls; when the user selects a partner-labeled model, the listed model provider also transiently processes the content needed for inference; Ghostbuild does not enable AI Gateway request logging and does not use AI Gateway to retain request payloads or request metadata; transcript and project copies remain in their separately inventoried stores",
      "currentExport": "there is no AI Gateway log export because request logging is disabled; persisted transcript and project records live in the separately inventoried user-owned stores and are outside the operator-held account export",
      "currentDeletion": "there is no Ghostbuild AI Gateway request log to delete; persisted source records and provider handling remain subject to their separately described controls"
    },
    {
      "system": "User-owned workspace runtime and Computer observability",
      "owner": "user Cloudflare account",
      "categories": [
        "60%-sampled workspace Worker invocation and diagnostic logs",
        "runtime, project, status, timing, binding, and exception metadata",
        "enabled Computer container operational logs"
      ],
      "secrets": "prompts, source code, credentials, capability tokens, and raw tool output are not intended log fields and must be excluded from deliberate diagnostic events",
      "currentRetention": "logs remain in the connected Cloudflare account under that account's plan, product settings, permissions, and Cloudflare retention controls; Ghostbuild does not copy them into the operator-account log store",
      "currentExport": "not included in a Ghostbuild account export; the user controls access through the connected Cloudflare account and its available provider tools",
      "currentDeletion": "project or runtime teardown does not immediately erase provider-retained logs; Cloudflare account controls and retention boundaries govern their removal"
    },
    {
      "system": "Browser storage",
      "owner": "user device",
      "categories": [
        "in-memory chat, transcript, and workspace presentation caches",
        "theme and builder-model preferences",
        "pending prompt in tab-scoped session storage",
        "asset-recovery and telemetry journey session state",
        "optional product-telemetry preference"
      ],
      "secrets": "the pending prompt can contain user content and remains in tab-scoped session storage",
      "currentRetention": "browser-controlled; in-memory caches end with the page, pending-prompt and other sessionStorage state lasts until the tab session ends, and preferences in localStorage persist until cleared or replaced",
      "currentExport": "no server export includes browser-owned data",
      "currentDeletion": "users must clear Ghostbuild site data with browser controls; the settings \"Your data\" card names the ghostbuild_session cookie, the ghostbuild_theme and ghostbuild_builder_model preferences, the telemetry preference, and tab-scoped session state, and warns that this must be repeated in every browser and profile; logging out or completing control-plane erasure disposes in-memory collections in that browser; no server-side request can enumerate or erase all browser-owned storage"
    },
    {
      "system": "User-owned deployed Cloudflare resources",
      "owner": "user Cloudflare account",
      "categories": [
        "Workers",
        "D1 databases",
        "R2 buckets",
        "KV namespaces",
        "Containers",
        "Durable Objects",
        "Agents"
      ],
      "secrets": "deployed bindings and secrets must never be exported by Ghostbuild",
      "currentRetention": "generated resources remain in the user's account while the project is active; project deletion queues bounded provider cleanup after a 30-minute grace period, and provider failures or unavailable authorization can delay retries",
      "currentExport": "deployment inventory is not available as an account export; the operator control plane does not know which apps a user has, so the self-service account export cannot and does not list them",
      "currentDeletion": "project deletion durably queues cleanup derived from every deployment plan: Workers are force-deleted with their script-owned Durable Objects, D1 databases and KV namespaces are deleted, and R2 objects are emptied in bounded batches before bucket deletion; missing resources are idempotent success and failed cleanup is retried while authorization remains available; control-plane account erasure deliberately deletes none of these resources and the settings control says so before it is confirmed"
    },
    {
      "system": "User-owned generated-application observability",
      "owner": "user Cloudflare account",
      "categories": [
        "60%-sampled generated-application Worker invocation and diagnostic logs",
        "5%-sampled generated-application Worker traces",
        "request, response, timing, binding, and exception metadata produced by the generated application"
      ],
      "secrets": "the user controls generated-application behavior and must avoid logging credentials, personal data, or unnecessary request and application content",
      "currentRetention": "logs, traces, and aggregate metrics remain in the connected Cloudflare account under that account's plan, product settings, permissions, and Cloudflare retention controls; Ghostbuild does not copy them into the operator-account log or trace store",
      "currentExport": "not included in a Ghostbuild account export; the user controls access and any available export through the connected Cloudflare account",
      "currentDeletion": "deployment removal does not immediately erase provider-retained observability; the user and Cloudflare account controls govern deletion, and Cloudflare automatically expires retained records at applicable boundaries"
    },
    {
      "system": "GitHub support and private security intake",
      "owner": "DOUS SOFTWARE INC. (controller); repository maintained through ferdousbhai",
      "categories": [
        "GitHub account and issue or report metadata",
        "public support issue content and attachments",
        "private vulnerability report content and attachments",
        "response and repository audit history"
      ],
      "secrets": "the public support form instructs requesters not to submit credentials, personal data, exploit details, or sensitive project content; vulnerability details belong only in GitHub private vulnerability reporting",
      "currentRetention": "public issues and private vulnerability reports remain under GitHub policies and repository controls while needed for support, security, fraud prevention, legal compliance, or claims",
      "currentExport": "no Ghostbuild self-service export exists for this system and the self-service account export does not reach it; public issues are publicly readable, private reports are limited by GitHub repository permissions, and manual requests remain subject to identity verification and provider controls",
      "currentDeletion": "the operator may minimize or remove records through available GitHub controls when no longer needed, subject to provider behavior and lawful retention exceptions"
    },
    {
      "system": "Cloudflare Email Routing and HEY contact mailbox",
      "owner": "DOUS SOFTWARE INC. (controller)",
      "categories": [
        "sender and recipient addresses",
        "support and privacy message content and attachments",
        "routing and delivery metadata",
        "case identifiers and response history"
      ],
      "secrets": "once activated, requesters will be instructed not to send credentials, recovery codes, identity documents, exploit payloads, or unnecessary project content; the private forwarding destination must never be committed",
      "currentRetention": "once activated, closed support email will be reviewed after 90 days and deleted when no longer needed; privacy and security case content will be minimized and reviewed annually while a legal, safety, fraud, or claims need remains; provider backup and routing metadata follow provider controls",
      "currentExport": "provisioned before launch but not advertised or accepted as rights-request intake until an external receive/reply drill passes; once activated, requests are handled manually after proportionate identity verification",
      "currentDeletion": "no case data is expected before activation; once activated, deletion is handled manually subject to identity verification, provider controls, and lawful retention exceptions"
    },
    {
      "system": "Ghostbuild control-plane Worker observability",
      "owner": "DOUS SOFTWARE INC. (controller); held in the operator Cloudflare account",
      "categories": [
        "sampled request method and path, Cloudflare request metadata, response status, timing, binding activity, and diagnostic metadata",
        "allowlisted product events",
        "opaque journey and per-error-event IDs",
        "status and bounded numeric metrics",
        "transient Cloudflare client IP used as the 60-second telemetry rate-limit key"
      ],
      "secrets": "telemetry requests omit browser credentials; prompts, code, credentials, tokens, URLs, user identifiers, raw tool output, and the client IP rate-limit key are rejected or omitted from the application event log",
      "currentRetention": "the client IP keys the configured 60-second rate-limit window; the verified production plan retains 60%-sampled control-plane Workers Logs and 5%-sampled traces for seven days, aggregate Worker metrics can remain up to three months, and Ghostbuild has no Logpush destination or tail consumer and does not copy logs or traces to another store",
      "currentExport": "the self-service account export returns control-plane records, not control-plane logs or traces; there is no self-service export of this observability data and application telemetry payloads carry no account identifier to select it by",
      "currentDeletion": "Cloudflare automatically deletes logs at the plan retention boundary; application telemetry payloads contain no account identifier; an August 4, 2026 readback showed access limited to the sole account administrator"
    }
  ],
  "launchBlockers": [
    "drill one synthetic privacy-rights request for the narrow operator-held D1 dataset, including identity verification, secret exclusion, case evidence, and Time Travel recovery boundaries",
    "decide and record whether self-service control-plane erasure changes the published retention criteria for user-owned Cloudflare resources, which Ghostbuild still never deletes on account erasure",
    "complete external receive/reply drills for support@ and privacy@ and confirm mailbox deletion behavior"
  ],
  "postLaunchHardening": [
    "improve user-owned runtime cleanup, receipts, and status without silently deleting generated deployments or other customer-controlled resources"
  ]
}
